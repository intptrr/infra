---
# Example inventory. Replace hosts and connection details with your own.
all:
  hosts:
    local:
      ansible_connection: local
      ansible_host: 127.0.0.1
    example:
      ansible_host: 192.0.2.10
      ansible_user: root
      # ansible_port: 22
      # ansible_ssh_private_key_file: ~/.ssh/id_ed25519
  children:
    servers:
      hosts:
        example:
